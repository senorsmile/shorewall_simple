{# ICMP Ping is dropped by default, unless we permit it per: #}
{# ftp://shorewall.net/archives/website/1.3/ping.html #}
{% if shorewall_default_policy is defined and shorewall_default_policy != "DROP" %}
run_iptables -A icmpdef -p icmp --icmp-type 8 -j {{ shorewall_default_policy }}
{% endif %}
